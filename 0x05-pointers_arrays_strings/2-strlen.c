#include "main.h"
#include <string.h>
/**
 * _strlen - length of string
 * Description: how meny char in astring
 * @s: parameter
 *
 * Return: 0 if success
 */
int _strlen(char *s)
{
	int i = strlen(s);

	return (i);
}
