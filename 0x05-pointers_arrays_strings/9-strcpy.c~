#include <string.h>
#include "main.h"

/**
 * _strcpy - copy string
 * @dest: the string will get the copy
 * @src: the string will be copied
 *
 * Description: this function is using strcpy func
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (s);
}
