#include <stdio.h>
#include "main.h"


void print_array(int *a, int n)
{
	int i;

	i = 0;
	if (i < n)
	{
		for (; i < n; i++)
		{
			printf("%d", a[i]);
			if (i != n - 1)
			{
				printf(", ");
			}
		}
	}
	printf("\n");
}
